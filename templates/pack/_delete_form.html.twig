<br> 
<br>
<form method="post" action="{{ path('app_pack_delete', {'idPack': pack.idPack}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pack.idPack) }}">
    <button class="btn"><i class="fa fa-trash"></i></button>

</form>

