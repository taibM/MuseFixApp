{% include 'Navbaradmin.html.twig' %}

    {% block css %}
        <link rel="stylesheet" href="{{ asset('https://fonts.googleapis.com/css?family=Open+Sans:300,400,600') }}">
        <link rel="stylesheet" href="{{ asset('css2/fontawesome.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css2/fullcalendar.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css2/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css2/tooplate.css') }}">
    {% endblock %}




{% block body %}
    <h1 style="padding-left: 650px;padding-top: 20px">User</h1>

    <table class="table">
        <tbody>
            <tr>
                            <th>Userid</th>
                            <td>{{ user.userid }}</td>
                        </tr>
                        <tr>
                            <th>Nom</th>
                            <td>{{ user.nom }}</td>
                        </tr>
                        <tr>
                            <th>Prenom</th>
                            <td>{{ user.prenom }}</td>
                        </tr>
                        <tr>
                            <th>Email</th>
                            <td>{{ user.email }}</td>
                        </tr>
                        <tr>
                            <th>Passwd</th>
                            <td>{{ user.passwd }}</td>
                        </tr>
                        <tr>
                            <th>Signupdate</th>
                            <td>{{ user.signupdate ? user.signupdate|date('Y-m-d H:i:s') : '' }}</td>
                        </tr>
                        <tr>
                            <th>Role</th>
                            <td>{{ user.role }}</td>
                        </tr>
                        <tr>
                            <th>Adresse</th>
                            <td>{{ user.adresse }}</td>
                        </tr>
                        <tr>
                            <th>Tel</th>
                            <td>{{ user.tel }}</td>
                        </tr>
            <tr>
            <th>Retour</th>
            <td><a href="{{ path('app_user_index') }}" style="color: white">Retour à la liste</a></td>
            </tr>
            <tr>
                <th>Modifier</th>
                <td><a href="{{ path('app_user_edit', {'id': user.userid}) }}" style="color: white">Modifier</a></td>
            </tr>
            <tr>
                <th>Supprimer</th>
                <td> {{ include('user/_delete_form.html.twig') }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
